---
import Layout from '../layouts/Default.astro'

import Topper from '../components/home/Topper';
import About from '../components/home/About';
import SocialBar from '../components/home/SocialBar.astro';
import LatestPosts from '../components/home/LatestPosts.astro';

interface MarkdownFrontmatter {
	date: number;
}

// let allPosts = await Astro.fetchContent('./posts/*.md');
// allPosts = allPosts.sort((a, b) => new Date(b.date).valueOf() - new Date(a.date).valueOf());

---

<Layout>
	<Topper client:only="preact" />
	<About client:only="preact" />
	<LatestPosts />
	
	<SocialBar />
</Layout>
