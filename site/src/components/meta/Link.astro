---
export interface Props {
  rel: string;
  href: URL | string;
  type?: string;
  title?: string;
  sizes?: string;
}
const { rel, href, ...rest } = Astro.props;
---

<link rel={rel} href={href} {...rest} />
